# Copy to .env and fill in. All are OPTIONAL — with none set, the pipeline runs
# fully offline (MockLLM + HashingEmbedder).

# Anthropic Claude — enables real relevance judging, claim extraction, scoring.
ANTHROPIC_API_KEY=

# Semantic Scholar — optional; raises the shared rate limit for enrichment.
S2_API_KEY=
