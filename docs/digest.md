# hexgen digest — 2026-09-15

**12** new backlog item(s) in the last 7 day(s).

## New this window

- **The paper establishes finite-sample risk bounds for mixed training (synthetic and real...** · score 0.074
  - _Contributions:_ The paper proves that mixed training (synthetic and real data combined throughout SGD) induces strong model collapse -- a persistent excess-risk floor -- while two-stage training, which restricts synthetic data to an initial stage, avoids this floor entirely, showing collapse is not inevitable under a simple data curriculum. It further derives scaling laws under a random sketch model showing larger models can amplify synthetic-induced degradation under mixing, quantifies how high-quality synthetic pretraining can reduce bias in two-stage training, and gives an exact finite-sample condition for two-stage training to strictly outperform real-data-only training.
  - _Applicable to our work:_ hexgen trains on a mixture of synthetic 'inverse factory' (B-rep, program) pairs and real, auto-labeled CAD parts -- exactly the mixed-vs-staged synthetic/real training regime this paper analyzes. Its core finding -- that mixing synthetic and real data throughout training can induce a persistent excess-risk floor (model collapse) that a two-stage curriculum (synthetic first, then real-only fine-tuning) avoids -- is directly actionable: it suggests hexgen should consider a staged synthetic-then-real curriculum (or at least measure for a collapse floor under its current mixed training) rather than assuming naive mixing is safe, and its finite-sample condition for when synthetic data helps gives a concrete criterion for how much/what-quality synthetic 'inverse factory' data to keep relative to real CAD data (care area 2: data-diversity vs data-volume tradeoffs for geometric generation).
  - _Additional context:_ This is a theory paper in linear regression, not a directly applicable algorithm -- transfer to hexgen's nonlinear graph-transformer setting is by analogy only. Still, the qualitative finding (mixed synthetic/real training can induce a collapse floor; staged training avoids it) is a specific, testable hypothesis worth checking empirically against hexgen's own synthetic-vs-real training mixture and curriculum ordering.
  - _Impact_ 0.50 · _Applicability_ 0.72 · _Confidence_ 0.41 · _Effort_ 2.0/10
  [2609.09572](https://arxiv.org/abs/2609.09572)
- **The paper introduces the canonical region graph: (1) canonical regions merge faces that...** · score 0.036
  - _Contributions:_ The paper shows that popular B-rep encoders (UV-Net, AAGNet, BRepNet) are not robust to variation in B-rep partitioning, surface encoding, or rigid-body pose for the same underlying solid, with accuracy collapsing under standard-benchmark perturbations, kernel import/export round-trips, and a new human-authored FreeCAD dataset of designers modeling the same part differently. It proposes the canonical region graph, an input representation whose nodes/features/coordinate frame are derived from the solid itself, proves theoretical invariance guarantees to repartitioning and rigid motion, and shows it matches the strongest baseline on clean benchmarks while remaining stable under every tested perturbation, outperforming data augmentation as a fix.
  - _Applicable to our work:_ Highly applicable: hexgen's graph-transformer encoder reads a B-rep face/edge/vertex graph and hexgen explicitly has a closed 'rotation-invariant encoder' line -- this paper's canonical region graph offers a concrete, proven alternative/complementary mechanism (canonical frame from centroid + principal axes, scale normalization, exact-integral features) for rigid-motion invariance, plus a second axis of robustness hexgen does not appear to address: invariance to B-rep re-partitioning/re-triangulation artifacts, which is exactly the kind of domain-gap/tokenization-mismatch issue relevant when training on synthetic 'inverse factory' B-reps vs auto-labeled real CAD parts that may be partitioned differently by different kernels or construction templates. The face-merging-into-region-nodes idea could directly reduce spurious variation in hexgen's pointer/copy targets (addressing congruent/split faces) since multiple raw faces of one underlying surface collapse to one canonical node.
  - _Additional context:_ Directly measures the same kind of representation robustness problem hexgen would face if trained on both synthetic inverse-factory B-reps and auto-labeled real CAD parts from different construction templates/kernels -- worth reading in full. Caveat: the method loses information that might correlate with 'how a part was built' (their Fusion-360 operation-labeling ablation), which is analogous to a concern for hexgen if any of its target programs are meant to reflect specific construction history rather than pure final-shape validity; also their pointer targets are per-region not per-original-face, which would require adapting hexgen's pointer/copy head to point at canonical regions instead of raw B-rep faces (or mapping region predictions back onto faces, as they do for segmentation).
  - _Impact_ 0.55 · _Applicability_ 0.84 · _Confidence_ 0.47 · _Effort_ 6.0/10
  [2609.11573](https://arxiv.org/abs/2609.11573)
- **CLAMP is a multimodal constraint-grounding framework: it uses the initial scene observa...** · score 0.031
  - _Contributions:_ The paper introduces CLAMP, a decoding-time constrained-generation framework for embodied planning with frozen VLMs that combines hard token masking (from scene-grounded object references and a symbolic action model) with an HMM-based lookahead that reweights candidates by precondition and goal-reachability feasibility. It shows on VLABench, SafeAgentBench, and TaPA that these scene-grounded constraints improve object grounding and safety in generated plans, with most remaining failures attributed to perception errors or misaligned constraint specifications.
  - _Applicable to our work:_ The decoding-time hard-mask + HMM-based feasibility-reweighting mechanism is directly transferable to hexgen's core structural-validity problem (care area 1): masking out program tokens that would reference faces not present in the input B-rep or violate a hex-meshing command's preconditions, and reweighting remaining candidates by a lookahead estimate of whether the partial program can still reach a state that will pass the frozen verifier (analogous to CLAMP's 'goal reachability'). The test-time, label-free adaptation of the feasibility model to unseen environments is also a plausible mechanism for domain-gap mitigation (care area 2) between the synthetic 'inverse factory' training distribution and real, auto-labeled CAD parts.
  - _Additional context:_ Only the abstract was available in this batch (no full-text sections), so architectural and quantitative detail is limited -- how the HMM is structured/trained, and numeric results per benchmark, are not extractable from this text. Worth a follow-up read of the full paper given the strong conceptual fit to hexgen's structural-validity and domain-gap problems.
  - _Impact_ 0.60 · _Applicability_ 0.66 · _Confidence_ 0.39 · _Effort_ 5.0/10
  [2609.08602](https://arxiv.org/abs/2609.08602)
- **ThinkPrior is a zero-rollout difficulty prior: before training starts, a small off-the-...** · score 0.021
  - _Contributions:_ The paper identifies and quantifies the cost of 'silent groups' in GRPO-based RLVR (up to 39% of rollouts contribute no gradient) and shows the problem is worse early in training because per-prompt difficulty is unknown ('cold start'). It introduces ThinkPrior, a zero-rollout difficulty prior built from an external, offline anchor pass, and shows on Qwen2.5-Math-7B across sixteen seeds that it more than halves early silent groups and cuts wasted rollouts through step 30 by nearly a fifth, while final accuracy is unchanged; a ThinkPrior+DAPO composition reduces total generated rollouts by 10.6% at matched update budget.
  - _Applicable to our work:_ hexgen's training data (inverse-factory-sampled and auto-labeled real-CAD programs) likely has a wide range of per-example difficulty; if hexgen is ever trained with RLVR-style updates against the frozen verifier's pass/fail signal (a natural extension of care area 6, self-play/verifier-in-the-loop training), ThinkPrior's zero-rollout, offline difficulty prior -- scored by a cheap proxy model or heuristic and used to avoid spending best-of-N verifier calls on B-reps whose programs are already trivially easy or all-fail -- is a directly transferable curriculum/data-selection mechanism to reduce wasted verifier calls during training, especially in the cold-start phase before the model has any track record on a given B-rep.
  - _Additional context:_ Domain and reward structure (math RLVR with GRPO, verifiable scalar reward) are structurally close to how hexgen could train against its frozen verifier, making this one of the more directly transferable papers in this batch despite the different application area. The headline result is explicitly a compute-reallocation, not an accuracy improvement, so the practical payoff for hexgen would be training-efficiency rather than backlog quality per se.
  - _Impact_ 0.30 · _Applicability_ 0.48 · _Confidence_ 0.45 · _Effort_ 3.0/10
  [2609.09075](https://arxiv.org/abs/2609.09075)
- **The paper models best-of-N selection as joint-Gaussian to derive verifier-gold correlat...** · score 0.020
  - _Contributions:_ The paper proves verifier-gold correlation rho is the exact exchange rate between test-time compute and capability, with an unsound verifier paying a polynomial penalty N^(1/rho^2). It demonstrates, in program-synthesis testbeds and a pre-registered scaled replication, that unsound/frozen verifiers lose Soundness-under-Pressure and get hacked under best-of-N optimization while a sound or reality-settled verifier improves monotonically and closes the reward-hacking gap. With real LLM judges and unit-test execution as gold on MBPP/HumanEval, it further shows a weak judge loses soundness under best-of-N while a settlement model anchored to executed outcomes reduces judge pricing error, and that under real GRPO training a frozen reward model's executed reward collapses while a periodically-refit settled reward model preserves most of it.
  - _Applicable to our work:_ This is a direct hit on hexgen's own training design: hexgen screens best-of-N candidate programs with a FROZEN machine-checkable verifier, and this paper's central finding is that unsound/frozen verifiers lose Soundness-under-Pressure and get hacked as best-of-N optimization pressure grows, while reality-anchored 'settlement' (periodically re-anchoring the verifier/reward to executed ground truth instead of keeping it permanently frozen) closes the hacking gap and preserves most of the executed reward under live RL training. Even though hexgen's verifier is deterministic/machine-checkable rather than a learned judge, the paper's diagnostic (measure Soundness-under-Pressure as N grows) and its settlement-mechanism template are directly transferable if hexgen ever adds a learned critic/value model alongside the frozen verifier, or loosens/learns part of the verifier to reduce false negatives.
  - _Additional context:_ Largely theoretical/synthetic (small DSL programs, not CAD geometry); the paper's own Limitations section flags that the real-model results cover only one model family with heavy benchmark saturation and untested learning adversaries, so treat the specific numbers as suggestive rather than a validated recipe. The core actionable idea for hexgen is the diagnostic framing (soundness-under-pressure vs. best-of-N) and the general principle of not leaving a verifier permanently frozen if it can instead be periodically settled against new ground truth.
  - _Impact_ 0.60 · _Applicability_ 0.66 · _Confidence_ 0.41 · _Effort_ 8.0/10
  [2609.09776](https://arxiv.org/abs/2609.09776)
- **QUAKE-CD represents binary change masks as grammar-constrained quadtree token sequences...** · score 0.018
  - _Contributions:_ The paper introduces QUAKE-CD, which recasts dense change-mask prediction as syntax-verifiable structured generation using a quadtree grammar, and QUAKE-CoT, a paired dataset of grounded chain-of-thought reasoning traces. It trains via curriculum SFT then grammar-gated dual-reward RL, achieving 78.31% accumulated F1 on QUAKE-CoT and clear gains over decoder-based and flat text-as-mask VLM baselines while producing more faithful bi-temporal reasoning.
  - _Applicable to our work:_ The core transferable mechanism is grammar-constrained, syntax-verifiable structured token generation for autoregressive decoders: a formal grammar with a single-pass linear-time parser gate that validates generated tokens, plus a rule-based structural reward computed directly from the decoded output, is directly analogous to constrained decoding for hexgen's DSL programs (care area 1) -- e.g. gating each program token against the DSL grammar during generation, and using a decoded-and-checked structural score as an RL reward alongside (or instead of relying solely on) the downstream frozen verifier. Their grammar-gated dual-reward RL (rule-based structural reward + learned semantic-quality reward) is a template for combining hexgen's frozen machine-checkable verifier with a learned critic reward.
  - _Additional context:_ Domain is remote-sensing change detection, not CAD/mesh generation, so nothing about B-rep encoding, pointer/copy mechanisms, or 3D geometry transfers directly -- only the general grammar-constrained-decoding + dual-reward RL recipe. I did not review the appendix Failure Mode Analysis or Licenses/Limitations sections in this pass, so paper-stated limitations are not captured here.
  - _Impact_ 0.50 · _Applicability_ 0.54 · _Confidence_ 0.39 · _Effort_ 6.0/10
  [2609.09876](https://arxiv.org/abs/2609.09876)
- **A minimal self-play implementation of Approximate Value Iteration (AVI) using only epsi...** · score 0.015
  - _Contributions:_ The paper provides the first systematic oracle-based comparison of neural AVI and MCTS-based self-play (AlphaZero) across multiple non-trivial games, measuring value error, policy regret, and play against perfect oracles. It shows AVI learns substantially more accurate value functions than an AlphaZero baseline, that its one-step-lookahead greedy policies remain competitive with MCTS-based policies at much lower training/inference cost, and that AVI trains stably on larger games (Othello, Go 9x9), with learned values that improve MiniZero's playing strength when substituted into the same MCTS inference budget.
  - _Applicable to our work:_ hexgen's frozen verifier plus best-of-N screening is itself a form of self-play/verifier-in-the-loop training (care area 6); this paper suggests that a much simpler direct value-learning signal (a learned critic trained by one-step negamax-style backups over partial hex-meshing program prefixes, scored against verifier outcomes) could substitute for or complement full MCTS/beam search over program prefixes (care area 1) at substantially lower training and inference cost, while still improving decision quality if later combined with search at deployment time (the cross-inference finding). The finding that error propagation may behave better in alternating/adversarial value backups is a useful prior if hexgen ever frames program construction as an adversarial or verifier-vs-generator game.
  - _Additional context:_ Domain is board games with much larger action/state spaces explored via self-play against a fixed opponent (or itself), not autoregressive program generation against a static verifier -- the negamax/alternating-player framing doesn't map cleanly onto single-agent program synthesis, so the transfer is at the level of 'simple value learning can rival search-heavy methods' rather than a directly reusable algorithm. Authors themselves caution the stability result is empirical, not proven, and may be specific to the alternating-game structure.
  - _Impact_ 0.40 · _Applicability_ 0.42 · _Confidence_ 0.45 · _Effort_ 5.0/10
  [2609.09094](https://arxiv.org/abs/2609.09094)
- **Each irrep block of degree l in an equivariant linear layer shares a channel-mixing mat...** · score 0.013
  - _Contributions:_ The paper identifies and characterizes an intra-layer learning-rate mismatch specific to equivariant networks: different irrep blocks receive different spectral step sizes under a single global Adam learning rate because of their differing gradient-rank structure. It introduces a parameter-free blockwise normalization rule that removes this mismatch, and shows in a controlled SO(3)-equivariant model and an e3nn interatomic potential model (trained on rMD17 and MD22) that combining block normalization with tuned Adam momentum coefficients makes Adam competitive with or better than Muon, explaining much of Muon's previously unexplained advantage on equivariant architectures.
  - _Applicable to our work:_ hexgen's encoder is explicitly a graph transformer over B-rep faces (care area 4) and the problem statement notes the 'rotation-invariant encoder' line is closed but leaves room for 'genuinely new mechanisms or deployment-time tricks' (care area 5); if the B-rep encoder or any component ever uses an SO(3)-equivariant architecture (e.g. an equivariant geometric feature extractor over face/edge coordinates), this paper's diagnosis (a single global learning rate under-trains high-degree irrep blocks relative to low-degree ones) and its free, drop-in blockwise-normalization fix are directly actionable and essentially free to try -- a training-stability lever rather than an architectural change, requiring no new equivariance mechanism itself.
  - _Additional context:_ This is an optimizer-diagnosis paper for pre-existing equivariant architectures (e3nn-style), not a new equivariant architecture or CAD/B-rep-specific method, so it only applies if hexgen's pipeline actually adopts an SO(3)/E(3)-equivariant layer somewhere; if the encoder stays a standard (non-equivariant) graph transformer, this finding is not applicable. Worth flagging to whoever owns training-stability tuning if equivariant components are ever introduced.
  - _Impact_ 0.20 · _Applicability_ 0.30 · _Confidence_ 0.45 · _Effort_ 2.0/10
  [2609.08381](https://arxiv.org/abs/2609.08381)
- **Probe-driven TTRL: generate output-free probe inputs from the problem statement, execut...** · score 0.013
  - _Contributions:_ The paper formulates probe-driven TTRL for code generation, using self-generated, output-free probe inputs to build a behavioral consensus reward (PCR) without oracle tests. It shows PCR is a noisy, asymmetric signal -- more reliable as a negative indicator than a correctness certificate -- and introduces ERPO, a rank-masked, entropy-regularized policy optimization algorithm that exploits this asymmetry. On coding benchmarks (LiveCodeBench, CodeContests) ERPO improves both pass@1 and the pass@k frontier, including in zero-shot transfer, and pass@1 with a single sample exceeds training-free Best-of-n baselines even at n=32.
  - _Applicable to our work:_ The core idea -- constructing a behavioral consensus reward from execution agreement across self-generated probes, then using it only as a conservative negative training signal via rank-masking -- is a plausible template for training hexgen when the frozen verifier's checks are expensive or when supervised imitation data runs out: candidate hex-meshing programs could be grouped, executed by the deterministic mesher, and rank-masked GRPO-style updates could suppress clearly-bad rollouts without over-trusting agreement among candidates as a correctness certificate (directly relevant to care area 6, self-play/verifier-in-the-loop training, and complementary to the existing best-of-N + frozen verifier screening).
  - _Additional context:_ This targets general code generation (Python-style competitive programming), not CAD/geometric DSLs, so the probe-generation mechanism (LLM invents test inputs from a natural-language problem statement) doesn't transfer directly -- hexgen already has a much stronger, deterministic verifier (compiles, zero inverted elements, quality bounds) rather than a noisy self-consistency proxy, so PCR's core motivation (no oracle available) is less pressing here. The transferable piece is narrower: the rank-masking/entropy-ceiling recipe as a stable way to do RL against a partially-trusted signal, useful if the pipeline ever adds a softer reward on top of the frozen verifier.
  - _Impact_ 0.35 · _Applicability_ 0.48 · _Confidence_ 0.45 · _Effort_ 6.0/10
  [2609.09135](https://arxiv.org/abs/2609.09135)
- **A test-verify-revise scaffold separates test construction from source repair: a Test ag...** · score 0.012
  - _Contributions:_ The paper introduces ExecCritic, a scaffold that keeps test generation and source repair as separate roles/agents so the validation evidence used to stop repair cannot be gamed by the same trajectory that wrote the patch. It trains a Test agent to produce reliable, discriminative repository-native tests and a Repair agent to revise source code from that fixed feedback via role-specific RL. On SWE-bench Verified, the composed trained system reaches 72.6% resolved, an 11.4-point gain over a no-test baseline, without using a stronger model or oracle feedback at evaluation time.
  - _Applicable to our work:_ The core structural idea -- keeping the artifact that judges correctness independent of and frozen relative to the artifact being generated/repaired -- maps directly onto hexgen's structural-validity problem: it suggests that a self-critiquing or self-testing loop for hex-mesh programs should not let the same model rollout that produced a candidate program also decide when to stop revising it, reinforcing the value of hexgen's already-frozen, machine-checkable verifier as the independent judge. The 'Learn to Test' role-specific RL recipe (rewarding valid, discriminative checks; separately rewarding feedback-conditioned revision) is a template for training a repair/edit-distillation policy (care area 1: DAgger-style learning to fix a model's own broken rollouts) that revises a hexgen program using structured feedback from the frozen verifier.
  - _Additional context:_ Domain is software repository repair (SWE-bench), not CAD/geometry, so nothing here transfers on data or architecture -- the transferable content is purely the training-loop design pattern (independent, frozen verification signal + specialized repair policy trained against it). Authors note the two-policy design adds engineering overhead versus a single shared-weight model; a hexgen adaptation would need to substitute the frozen mesh verifier for the learned Test agent, which is arguably simpler since hexgen's verifier is already deterministic and machine-checkable rather than learned.
  - _Impact_ 0.30 · _Applicability_ 0.42 · _Confidence_ 0.66 · _Effort_ 7.0/10
  [2609.09133](https://arxiv.org/abs/2609.09133)
- **Neural CVT replaces classical iterative CVT solvers with a learned recurrent optimizer....** · score 0.007
  - _Contributions:_ The paper introduces Neural CVT, a learning-based surface-CVT solver that couples a geometric feature extractor with a recurrent neural optimizer to predict seed displacements over a small number of steps, trained self-supervised on the CVT energy itself. It demonstrates generalization to unseen organic and CAD-like shapes, initialization strategies, and seed densities, producing isotropic, feature-preserving remeshes comparable to state-of-the-art offline optimizers while accelerating CVT optimization by one to two orders of magnitude.
  - _Applicable to our work:_ hexgen's mesher executes generated programs to produce all-hexahedral meshes and its verifier checks scaled-Jacobian/aspect-ratio quality and strict parametric surface-match boundary fidelity -- Neural CVT's core mechanism (a graph/point-cloud encoder + a small-step recurrent optimizer trained self-supervised directly on a classical geometric energy, replacing an expensive combinatorial reconstruction with a few learned steps) is a directly transferable pattern for building a fast learned proxy or initializer for parts of the hex-meshing pipeline (e.g., seed/vertex placement, quality-driven local geometry repair) rather than for the program-generation decoder itself; its demonstrated success on CAD-like models with sharp features (Figure 1) is a relevant existence proof that neural methods generalize to CAD geometry with hard mechanical-part features similar to hexgen's B-rep inputs.
  - _Additional context:_ This is a computer-graphics remeshing paper (triangle surface meshes and CVT), not a hex-meshing DSL/program-generation paper, so it does not bear on structural-validity-of-generated-programs or B-rep-to-program decoding directly; the value is as a design pattern for learned geometric solvers and as evidence that learned methods trained self-supervised on a classical quality energy can match offline optimizers on CAD-like inputs.
  - _Impact_ 0.25 · _Applicability_ 0.30 · _Confidence_ 0.65 · _Effort_ 7.0/10
  [2609.08497](https://arxiv.org/abs/2609.08497)
- **LASP (Language-Augmented Semantic Priors) is a semantic reasoning layer above existing...** · score 0.006
  - _Contributions:_ The paper introduces LASP, described as the first language-driven framework that predicts structured B-spline priors from symbolic CAD modeling histories via rich-text semantic reasoning and a two-stage LLM fine-tuning pipeline. It shows these language-derived priors substantially improve both parameter-level prediction accuracy (e.g. B-spline detection F1 0.95 vs 0.51-0.67 for baselines) and, when instantiated as solver-accessible configuration choices, downstream engine-level fitting quality (71.2% RMS and 85.6% Hausdorff distance reduction), evaluated on an extended ABC-dataset subset via OpenCascade.
  - _Applicable to our work:_ The core transferable mechanism is using an LLM/language-conditioned model to predict structured, solver-usable priors from a symbolic procedural history (construction-history-as-representation, our care area 3) that then bias a deterministic geometric solver's search -- directly analogous to using a learned model to propose structured parameters (e.g. block/sweep/projection command arguments) that steer hexgen's deterministic mesher toward valid, high-quality configurations. The two-stage local-then-global autoregressive prior prediction over a sequence of operations is also a reasonable template for conditioning program-token generation on accumulated context.
  - _Additional context:_ This is not directly about hex-meshing or pointer/copy networks over B-rep graphs -- it targets B-spline surface fitting parameters, not mesh-generation programs, and its 'verifier' is a downstream fitting-error metric rather than a hard machine-checkable pass/fail gate. The LLM backbone (Qwen3-14B) is far larger than hexgen's 27M-parameter model, so the approach as implemented is not directly reusable at hexgen's scale; the transferable idea is the semantic-prior-as-solver-configuration-bias pattern, not the specific architecture.
  - _Impact_ 0.25 · _Applicability_ 0.36 · _Confidence_ 0.45 · _Effort_ 7.0/10
  [2609.11708](https://arxiv.org/abs/2609.11708)

## Current top 10 backlog

1. **Discovery Loop is a ~400-line Python system implementing a single-candidate (no populat...** · score 0.105 · [2609.05093](https://arxiv.org/abs/2609.05093)
2. **HiFi-BRep is a two-stage VAE + latent-diffusion pipeline for B-Rep generation. The topo...** · score 0.093 · [2608.16485](https://arxiv.org/abs/2608.16485)
3. **Defines a per-edge structural invariant, edge-girth g_e (length of the shortest cycle t...** · score 0.078 · [2609.01441](https://arxiv.org/abs/2609.01441)
4. **RefineCut is a two-stage training recipe plus a closed-loop inference procedure for a c...** · score 0.077 · [2608.25622](https://arxiv.org/abs/2608.25622)
5. **A wall-first network reuses the Raster2Seq backbone (ResNet features + deformable-atten...** · score 0.075 · [2608.25608](https://arxiv.org/abs/2608.25608)
6. **SQL-Zero is a proposer-solver self-play framework in which a challenger and a solver bo...** · score 0.075 · [2609.04697](https://arxiv.org/abs/2609.04697)
7. **The paper establishes finite-sample risk bounds for mixed training (synthetic and real...** · score 0.074 · [2609.09572](https://arxiv.org/abs/2609.09572)
8. **SWYB (Stay Within Your Bounds) models the grammar as a pushdown automaton (PDA) and off...** · score 0.073 · [2608.28229](https://arxiv.org/abs/2608.28229)
9. **SRD generates N candidate trajectories, routes each to Keep/Refine/Discard via rank-nor...** · score 0.069 · [2608.24338](https://arxiv.org/abs/2608.24338)
10. **Qwen2.5-3B-Instruct is first adapted with field-weighted QLoRA supervision anchored to...** · score 0.069 · [2609.05221](https://arxiv.org/abs/2609.05221)
