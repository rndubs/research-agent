# hexgen digest — 2026-07-21

**15** new backlog item(s) in the last 7 day(s).

## New this window

- **Models the per-instance false-accept rate as a latent variable drawn from a distributio...** · score 0.058
  - _Contributions:_ The paper gives a minimal theory of partially correlated verifier cascades: modeling per-instance false-accept/true-accept rates as latent variables, it derives that posterior log-odds are concave in the number of gates k (with the independence-based Odds Law as just the tangent/upper bound), that failure can decay only polynomially instead of exponentially under correlation, and that a 'blind-spot' probability mass caps achievable reliability below 1 regardless of how many gates are added. It also shows the theory is empirically measurable and that independence-based extrapolation can underestimate failure by orders of magnitude at larger k.
  - _Applicable to our work:_ Directly applicable to designing/calibrating hexgen's own frozen verifier, which is itself a multi-gate cascade (compiles; zero inverted elements; scaled-Jacobian/aspect-ratio bounds; surface-match fidelity; feature-edge capture; element-budget sanity): this theory gives a principled way to estimate how correlated those gates are, whether adding more checks is still buying reliability or hitting a blind-spot ceiling, and suggests decorrelating checks (e.g. across independent geometric criteria) rather than stacking more of the same kind of check when screening best-of-N candidates.
  - _Additional context:_ This is a theoretical/methods note (builds on arXiv:2606.15712, the 'Odds Law' paper) rather than an applied CAD or program-synthesis paper; abstract-only extraction. The practical takeaway ('decorrelate, don't just add gates') is the most directly actionable piece for hexgen's verifier design.
  - _Impact_ 0.40 · _Applicability_ 0.72 · _Confidence_ 0.40 · _Effort_ 2.0/10
  [2607.13918](https://arxiv.org/abs/2607.13918)
- **For a fixed candidate pool, the paper decomposes a selector or verifier's net gain into...** · score 0.054
  - _Contributions:_ The paper introduces a diagnostic framework that decomposes the net gain of test-time collaboration methods (self-consistency, best-of-N, critics, verifiers) into oracle gap, signal fidelity, conditional selection quality, and harm to already-correct outputs, evaluated on LiveCodeBench, MATH Level-5, and GPQA-Diamond. It finds that gains are bounded first by the oracle gap and then by signal fidelity, and shows this varies substantially by verifier type and dataset, including cases where LLM selectors are net-negative. The framework yields a practical pre-deployment diagnostic for deciding whether to invest in test-time collaboration.
  - _Applicable to our work:_ Directly applicable diagnostic for hexgen's best-of-N plus frozen-verifier design: the oracle-gap / recoverable-mass / signal-fidelity / harm decomposition could be measured on hexgen's candidate program pools to check whether best-of-N sampling is actually paying off for a given part class, versus the pool lacking any valid program at all (low recoverable mass) or the verifier's discrimination being too weak to help -- directly targets the self-play/verifier-in-the-loop-training care area.
  - _Additional context:_ hexgen's verifier is deterministic and machine-checkable (not a noisy LLM verifier), so signal fidelity should be near-perfect by construction; the most relevant transferable piece is 'recoverable mass' -- checking whether best-of-N candidate pools for hard parts actually contain a structurally valid program before investing more sampling budget.
  - _Impact_ 0.40 · _Applicability_ 0.72 · _Confidence_ 0.38 · _Effort_ 2.0/10
  [2607.17531](https://arxiv.org/abs/2607.17531)
- **Introduces 'generative compilation': a 'sealor' transformation converts a partial progr...** · score 0.033
  - _Contributions:_ The paper introduces generative compilation, the first method to obtain real compiler feedback on partial programs during generation via a lightweight syntax-guided 'sealor' transform, proven correct (mechanized in Lean) on a core Rust-like calculus and then extended to a partial-program checker for real Rust. It shows this reduces non-compiling outputs and improves functional correctness on repository-level Rust tasks across frontier and open-weight models by catching errors early, close to their source, rather than only after full generation.
  - _Applicable to our work:_ The 'sealor' idea -- transform a partial program into something a checker can diagnose without prematurely rejecting completable prefixes -- is directly transferable to hexgen's structural-validity problem: instead of only screening complete best-of-N candidates with the frozen verifier, a similar completion transform could let the hex-meshing DSL compiler/mesher give early feedback on partial programs during autoregressive decoding, catching dead-end block/sweep/projection prefixes before a full rollout is wasted.
  - _Additional context:_ Abstract-only extraction (no full text/GROBID parse available yet). The core mechanism is language-agnostic in principle (syntax-guided completion + diagnosis), but the concrete sealor construction is specific to a Rust-like calculus/real Rust; porting it to a hex-meshing DSL would require redesigning the completion transform for that grammar. Worth a closer read of the full paper for the completion-transform design once code/paper text is available.
  - _Impact_ 0.70 · _Applicability_ 0.66 · _Confidence_ 0.43 · _Effort_ 6.0/10
  [2607.13921](https://arxiv.org/abs/2607.13921)
- **The paper proves that a broad class of hierarchical graph transformers combining global...** · score 0.029
  - _Contributions:_ The paper characterizes the expressive power of global-attention graph foundation models for MILPs, proving they are bounded by the 1-WL test regardless of parameter setting, and validates this across ten diverse encoder architectures (Graphormer-, GraphGPS-, Set-Transformer-, and Gasse-style). It further shows that expressiveness beyond 1-WL comes from input encoding rather than attention: random-walk positional encodings can separate 1-WL-equivalent instance pairs, though further constructions expose the limits of that remedy. The result yields an encoder-agnostic diagnostic for detecting 1-WL-induced representation equivalence.
  - _Applicable to our work:_ hexgen's graph-transformer encoder over B-rep face-adjacency graphs (Graphormer-style edge-biased attention) could suffer the same 1-WL expressiveness ceiling, meaning distinct-but-WL-equivalent faces could be mapped to identical embeddings before the pointer/copy head addresses them -- directly bearing on the 'symmetry/orbit ambiguity when pointing at congruent nodes' care area. The paper's diagnostic could be run on hexgen's encoder to test for such collapse, and its random-walk-positional-encoding remedy is a concrete, low-cost mechanism to try if collapse is found.
  - _Additional context:_ Domain studied is MILP graphs, not B-rep, but the architectural components (global linear attention, edge-weighted cross-attention, Graphormer/GraphGPS-style encoders) closely match hexgen's encoder family, so the WL-boundedness result plausibly transfers directly; only the abstract was available, so exact proof scope and remedy details are unverified.
  - _Impact_ 0.35 · _Applicability_ 0.66 · _Confidence_ 0.38 · _Effort_ 3.0/10
  [2607.17570](https://arxiv.org/abs/2607.17570)
- **EXPLORE integrates simulator-guided Monte Carlo Tree Search (MCTS) with transformer-bas...** · score 0.026
  - _Contributions:_ The paper presents EXPLORE, the first framework to combine structured test-time search (simulator-guided MCTS) with language-model decoding for analog circuit topology generation. By allocating simulator budget selectively to topology-altering decisions rather than uniformly across all tokens, it substantially raises success rate and lowers error over one-shot generation and sampling-and-filter baselines under a matched search budget.
  - _Applicable to our work:_ This is a close structural analog to hexgen's own best-of-N + frozen-verifier screening: EXPLORE's core idea of using MCTS guided by an expensive simulator, but only spending that simulator budget on the decoding steps that actually alter structure (topology-altering tokens) rather than every token, is directly transferable to hexgen's decoding of block/sweep/projection DSL programs -- the mesher/verifier could similarly be invoked selectively at structurally consequential decision points during search over program prefixes rather than only at full-candidate screening time, which is exactly care-area 1's 'MCTS or beam search over program prefixes' concern.
  - _Additional context:_ Application domain is analog circuit design, but the search-budget-allocation mechanism (LM priors to skip cheap/confident tokens, spend simulator calls on the hard topology decisions) is a concrete, quantitatively validated technique rather than just a conceptual analogy. Abstract-only extraction.
  - _Impact_ 0.65 · _Applicability_ 0.66 · _Confidence_ 0.42 · _Effort_ 7.0/10
  [2607.13416](https://arxiv.org/abs/2607.13416)
- **VRR-Stop uses a four-parameter noise model separating verifier false acceptance and fal...** · score 0.025
  - _Contributions:_ The paper proposes VRR-Stop, a robust stopping framework for noisy verify-repair-repeat loops in LLM agents that models verifier and repairer noise separately and decides whether to continue repairing based on the sign of the estimated marginal gain in validity. It also introduces VRR-Guard, an estimation-free fallback for when verifier discrimination is too weak for calibration to be reliable. On a GSM8K stress setting, VRR-Stop substantially improves final true validity over fixed-round repair while using fewer average repair rounds.
  - _Applicable to our work:_ Directly transferable stopping/decision framework for hexgen's best-of-N plus frozen-verifier plus repair pipeline: a VRR-Stop-style noise model and belief-filtering could decide when to keep repairing a candidate hex-mesh program versus commit or discard it, and VRR-Guard's margin-based fallback guards against acting on an unreliable signal, directly targeting the structural-validity and verifier-in-the-loop-training care areas.
  - _Additional context:_ hexgen's verifier is described as a FROZEN, machine-checkable, deterministic pass/fail (compiles, zero inverted elements, quality bounds, etc.), so it likely has much higher discrimination than the noisy LLM verifiers this paper targets -- the explicit noise model may be unnecessary overhead, but the general stopping-criterion framing (marginal-gain sign, cost-bounded repair, guard against low-signal regimes) remains a directly applicable design pattern for a repair/edit-distillation loop.
  - _Impact_ 0.50 · _Applicability_ 0.66 · _Confidence_ 0.38 · _Effort_ 5.0/10
  [2607.17641](https://arxiv.org/abs/2607.17641)
- **MxGPS (Multiplex GPS) runs K task-specialized graph transformer (GPS) branches over a s...** · score 0.016
  - _Contributions:_ The paper identifies 'topology overfitting' as a systematic failure mode of single-task GNN fine-tuning for power grids and introduces MxGPS, a multiplex graph-transformer foundation model with a shared node encoder and multiple task-specialized branches, jointly pretrained/fine-tuned on two complementary grid tasks. Under cross-validation across four unseen grid topologies, MxGPS degrades far less under topology shift than single-task baselines despite using far fewer parameters, showing that multi-task joint training is a parameter-efficient route to topology-agnostic generalization.
  - _Applicable to our work:_ The core diagnosis -- that task-specific fine-tuning on a graph transformer overfits to the relational structure of training-set graph topologies, causing severe degradation when the graph shape shifts -- is a strong structural analogy to hexgen's domain-gap problem (a graph-transformer encoder over B-rep face-adjacency graphs trained on synthetic 'inverse factory' topologies must generalize to real, differently-shaped CAD part graphs). The fix (shared encoder + multiple complementary joint tasks forcing the encoder off topology-specific shortcuts, rather than single-task fine-tuning) suggests hexgen could add a second, complementary self-supervised or auxiliary graph task during pretraining/fine-tuning of its encoder to reduce overfitting to the synthetic B-rep topology distribution.
  - _Additional context:_ Application domain (power grids) is different, but the graph-transformer + topology-shift generalization mechanism is a clean, quantitatively validated methodological bridge. Abstract-only extraction; would want the full paper to see exactly how the complementary tasks are chosen and whether that recipe generalizes beyond power-grid physics.
  - _Impact_ 0.50 · _Applicability_ 0.54 · _Confidence_ 0.41 · _Effort_ 7.0/10
  [2607.13763](https://arxiv.org/abs/2607.13763)
- **CASP (Certificate-Augmented Solution Pruning) has the predictor propose which parts of...** · score 0.014
  - _Contributions:_ The paper introduces CASP, a framework that augments learned predictions for offline NP-hard optimization with a sound polynomial-time verifier so that pruning decisions are certified rather than trusted outright. It develops accompanying learning theory showing the verifier bounds the induced loss class, giving certificate parameters a tilde-O(eps^-2 log K) sample complexity, versus no distribution-free rate (and an Omega(R/eps^2) lower bound under cost spread R) for unverified commitments. It shows verifiable-confidence filtering dominates a standard min-combiner with a closed-form margin, and that verified predictions remain useful even against degenerate LP optimal faces where symmetric policies stall. Experiments on five problems confirm that under distribution shift, unverified pruning can lose up to 26% of the optimum while the verified deployment loses nothing.
  - _Applicable to our work:_ The core mechanism -- never accept a learned prediction/pruning decision until a sound, cheap verifier has certified it, and prefer verifiable-confidence filtering over trusting raw model confidence -- is directly analogous to hexgen's design of screening best-of-N candidate programs with a frozen machine-checkable verifier. Their learning-theoretic result (verified filtering has a bounded, learnable loss class with better sample complexity than unverified commitment, and stays robust under distribution shift where unverified predictions lose up to 26% of optimum) is a useful theoretical argument for why verifier-guided best-of-N / rejection sampling should generalize better to real (out-of-distribution) CAD parts than trusting the decoder's raw scores alone, and could motivate using verifiable-confidence-style filtering (rather than plain likelihood) to rank candidate programs before verification.
  - _Additional context:_ Only the abstract was available for extraction (no full-text chunks were retrieved), so architecture/dataset details beyond the abstract's claims could not be grounded; the five test problems and full experimental setup are unknown. The theoretical framing (verified vs. unverified prediction-augmented optimization) is the main transferable idea rather than any concrete algorithm directly reusable in hexgen's pipeline.
  - _Impact_ 0.35 · _Applicability_ 0.54 · _Confidence_ 0.43 · _Effort_ 6.0/10
  [2607.14545](https://arxiv.org/abs/2607.14545)
- **Token-Level Off-Policy Labeling (TOPL) reframes post-training as a token-level correctn...** · score 0.013
  - _Contributions:_ The paper proposes TOPL, an off-policy post-training paradigm that trains a model to classify tokens in its own responses as good or bad rather than directly imitating off-policy sequences. On 11 document-summarization datasets it achieves strong out-of-distribution generalization against sequence-level and token-level baselines, and the approach transfers to machine translation. Ablations show the token-level learning signal is essential; sequence-level analogues do not confer the same benefit, and the learned LoRA adapters behave as interpretable linear classification heads and steering vectors.
  - _Applicable to our work:_ The core idea -- training the model to distinguish good/bad tokens at each generation step rather than only supervising fully correct sequences -- is a direct analogue to hexgen's structural-validity problem: a token-level (per-DSL-command) correctness signal derived from the verifier could give denser training signal about which partial command choices lead toward a compiling/valid program, potentially improving generalization to out-of-distribution real CAD parts (domain-gap care area) beyond pure best-of-N imitation.
  - _Additional context:_ Applied to text generation faithfulness (summarization/machine translation), not structured program synthesis, but the token-level correctness-prediction framing is architecture-agnostic and plausibly portable to hexgen's autoregressive DSL decoder and its pointer/coordinate heads.
  - _Impact_ 0.45 · _Applicability_ 0.48 · _Confidence_ 0.37 · _Effort_ 6.0/10
  [2607.17524](https://arxiv.org/abs/2607.17524)
- **AdaLook is an adaptive lookahead framework for masked diffusion language model (DLM) de...** · score 0.013
  - _Contributions:_ The paper proposes AdaLook, an adaptive multi-step lookahead decoding framework for diffusion language models that replaces shallow one-step lookahead with a variance-based, adaptive-depth rollout policy plus branch expansion at informative intermediate states. Experiments across various benchmarks and models show AdaLook achieves a better accuracy-vs-decoding-steps trade-off than existing one-step lookahead decoding methods.
  - _Applicable to our work:_ The core mechanism -- adaptively deciding how much value/critic-guided lookahead search to spend at each decode step, based on score variance, plus branch expansion at uncertain states -- is directly transferable to best-of-N / verifier-guided decoding search over hexgen's program prefixes: instead of fixed-depth beam or MCTS search, hexgen's decoder could spend more rollout budget only at command positions where candidate scores are uncertain (e.g. ambiguous face pointers or block/sweep choices), cutting compute while preserving structural-validity gains from deeper search.
  - _Additional context:_ Only the abstract was available (no full text chunks retrieved), so architectural and experimental details beyond the abstract are unknown; this is a decoding-time search technique for masked DLMs, not for autoregressive decoders like hexgen's, so the adaptation would need validation on autoregressive/verifier-scored program decoding rather than assumed to transfer directly.
  - _Impact_ 0.35 · _Applicability_ 0.48 · _Confidence_ 0.39 · _Effort_ 5.0/10
  [2607.15655](https://arxiv.org/abs/2607.15655)
- **Combines Taylor Series Imitation Learning (TaSIL) to mitigate policy-induced distributi...** · score 0.012
  - _Contributions:_ The paper proposes a distributionally robust and safe imitation learning framework that jointly addresses two distinct sources of distribution shift: policy-induced shift (via Taylor Series Imitation Learning) and uncertainty-induced shift (via distributionally robust adaptive control), formulated so the learned policy explicitly accounts for safety constraints under distributional uncertainty. It demonstrates the approach on a UAV task that must be completed in an uncertain environment while avoiding unsafe regions.
  - _Applicable to our work:_ Hexgen is itself trained via supervised imitation on synthetic 'inverse factory' rollouts and auto-labeled real CAD parts, and must generalize from that training distribution to arbitrary real parts -- exactly the policy-induced/uncertainty-induced distribution-shift setting this paper targets. The TaSIL mechanism for mitigating policy-induced shift during imitation learning is a candidate technique to make hexgen's decoder more robust when its own generated program prefixes drift from the training distribution of the inverse-factory and template-labeled demonstrations, and the safety-constraint framing maps naturally onto hexgen's hard verifier constraints (zero inverted elements, quality bounds) as constraints to respect under distribution shift rather than just imitate.
  - _Additional context:_ Application domain is UAV control, not CAD/geometry, so the transfer is at the level of the imitation-learning-under-distribution-shift mechanism (TaSIL) rather than any CAD-specific technique. Abstract-only extraction; no quantitative results are given in the abstract to assess strength of the claims.
  - _Impact_ 0.45 · _Applicability_ 0.48 · _Confidence_ 0.34 · _Effort_ 6.0/10
  [2607.13436](https://arxiv.org/abs/2607.13436)
- **The paper proves that for spherical-harmonic degrees l<=2, photometry is algebraically...** · score 0.011
  - _Contributions:_ The paper presents E3DGS, an SE(3)-equivariant architecture for 3D Gaussian Splatting built on a 'Color-as-Geometry' formulation: a Unified Matrix Embedding maps position, covariance, and spherical-harmonic color coefficients into a unified gl(3) carrier space so a single rigid-body-equivariant architecture can process all Gaussian attributes without expensive Clebsch-Gordan tensor products. Evaluations on object vision and action-conditioned Gaussian world modeling show strong robustness under camera-frame changes and improved data efficiency.
  - _Applicable to our work:_ The core idea -- unifying heterogeneous per-attribute rotation actions (vector, tensor, higher-order harmonic) into one matrix carrier space so a single equivariant architecture handles them all without Clebsch-Gordan products -- is a genuinely new, lower-cost mechanism for building a natively rotation-equivariant B-rep graph encoder: hexgen's face/edge/vertex graph carries multiple geometric attribute types (positions, normals, possibly surface-curvature tensors) whose rotation actions differ, and this unified-matrix-embedding trick could let a graph-transformer encoder be exactly SE(3)-equivariant across all of them cheaply, which is relevant to the rotation-robustness care area despite that line being otherwise closed to genuinely new mechanisms.
  - _Additional context:_ Only the abstract was retrieved (no full-text chunks), so implementation details of the Unified Matrix Embedding and the exact evaluation setup (which 'object vision' and 'Gaussian world modeling' benchmarks) are unknown; applicability is to the encoder side of hexgen (B-rep graph encoding), not directly to program decoding.
  - _Impact_ 0.50 · _Applicability_ 0.42 · _Confidence_ 0.38 · _Effort_ 7.0/10
  [2607.15536](https://arxiv.org/abs/2607.15536)
- **The framework combines an LLM for program synthesis with neural simulation-based infere...** · score 0.006
  - _Contributions:_ The paper presents a framework for joint model selection and parameter estimation that combines LLM-based program synthesis with neural simulation-based inference, enabling inference over a pool of candidate model structures rather than only parameters within one fixed model. Given an open-ended natural-language prompt, an LLM proposes and iteratively refines candidate simulator programs, which are scored with neural density estimation. The method is evaluated on benchmarks spanning deterministic dynamics, stochastic epidemic models, and dark matter substructure inference from gravitational-lensing images.
  - _Applicable to our work:_ The propose-mutate-evaluate loop -- an LLM proposing candidate programs that are iteratively refined and scored by an external evaluator -- is structurally analogous to hexgen's best-of-N-plus-verifier generation process and could inform an LLM-agent-authoring pipeline (care area 6) for synthesizing additional diverse (B-rep, program) training pairs beyond the inverse-factory sampler.
  - _Additional context:_ Applied to scientific simulator code synthesis (epidemic models, gravitational lensing), not CAD/geometry; the transferable piece is the propose-mutate-evaluate loop structure and use of an LLM as a program-synthesis proposal mechanism, not any CAD-specific content.
  - _Impact_ 0.30 · _Applicability_ 0.42 · _Confidence_ 0.32 · _Effort_ 7.0/10
  [2607.17540](https://arxiv.org/abs/2607.17540)
- **Nexus decouples mesh generation into two diffusion sub-problems: vertices are represent...** · score 0.006
  - _Contributions:_ The paper presents Nexus, a diffusion-based mesh generator that avoids autoregressive serialization by decoupling vertex generation (via octree-structured coarse-to-fine diffusion) from topology generation (via a continuous per-vertex embedding scheme called Spacetime Interval, generated with a second diffusion model). It reports that this holistic, non-sequential approach outperforms state-of-the-art autoregressive and two-stage mesh-generation baselines on standard 3D datasets and in a practitioner user study.
  - _Applicable to our work:_ Nexus targets triangle meshes rather than block-structured hex meshes and is not directly reusable, but its core motivating argument -- that sequential/autoregressive generation of mesh structure is error-prone and that decoupling geometry (vertices) from combinatorial structure (topology) into separate, non-autoregressive generative stages avoids this -- is a relevant structural alternative worth considering for hexgen's own DSL-program decoder, and 'Spacetime Interval' as a continuous encoding of discrete topological relationships could inspire an alternative (non-pointer) representation for face/edge adjacency targets.
  - _Additional context:_ Different mesh family (unstructured triangle vs. all-hex block-structured) and different generation problem (unconditional/image-conditioned free mesh generation vs. B-rep-conditioned DSL program synthesis), so the applicability is at the level of a design-philosophy argument (avoid autoregressive error accumulation) rather than a directly portable technique. Abstract-only extraction.
  - _Impact_ 0.35 · _Applicability_ 0.36 · _Confidence_ 0.40 · _Effort_ 9.0/10
  [2607.13563](https://arxiv.org/abs/2607.13563)
- **By reassociating radial projection, spherical-harmonic coupling, and graph aggregation,...** · score 0.002
  - _Contributions:_ The paper shows that the edge-sized intermediates conventionally materialized by equivariant tensor-product graph convolutions are artifacts of the execution schedule, not requirements of the operator itself. It introduces a streaming reformulation, implemented as a generated-CUDA backend called Sobek, that consumes edge-local products directly into bounded receiver-side state while preserving full multiplicity mixing and differentiability. Sobek is evaluated across edge-scaling regimes and two operator families against a prior state-of-the-art (OpenEquivariance).
  - _Applicable to our work:_ The streaming reassociation trick that avoids materializing edge-sized tensor-product intermediates is a genuinely new deployment-time mechanism relevant to the rotation/equivariance care area: if hexgen's encoder were ever extended with equivariant tensor-product layers over B-rep attributes, this would remove the memory bottleneck that otherwise limits scaling such layers.
  - _Additional context:_ Only the abstract was available. hexgen's B-rep graphs are tiny (~7-15 faces), so Sobek's edge-scaling memory benefits are unlikely to matter directly unless a much larger/denser representation or an equivariant-encoder line is adopted; relevance is speculative and forward-looking rather than immediately actionable.
  - _Impact_ 0.15 · _Applicability_ 0.24 · _Confidence_ 0.38 · _Effort_ 8.0/10
  [2607.18074](https://arxiv.org/abs/2607.18074)

## Current top 10 backlog

1. **Models the per-instance false-accept rate as a latent variable drawn from a distributio...** · score 0.058 · [2607.13918](https://arxiv.org/abs/2607.13918)
2. **For a fixed candidate pool, the paper decomposes a selector or verifier's net gain into...** · score 0.054 · [2607.17531](https://arxiv.org/abs/2607.17531)
3. **Introduces 'generative compilation': a 'sealor' transformation converts a partial progr...** · score 0.033 · [2607.13921](https://arxiv.org/abs/2607.13921)
4. **The paper proves that a broad class of hierarchical graph transformers combining global...** · score 0.029 · [2607.17570](https://arxiv.org/abs/2607.17570)
5. **OPSD-V is an on-policy self-distillation scheme: a student follows its own exact infere...** · score 0.028 · [2607.08766](https://arxiv.org/abs/2607.08766)
6. **EXPLORE integrates simulator-guided Monte Carlo Tree Search (MCTS) with transformer-bas...** · score 0.026 · [2607.13416](https://arxiv.org/abs/2607.13416)
7. **VRR-Stop uses a four-parameter noise model separating verifier false acceptance and fal...** · score 0.025 · [2607.17641](https://arxiv.org/abs/2607.17641)
8. **Study of Best-of-N zero-shot TTS candidate selection using an ASR verifier; shows verif...** · score 0.024 · [2607.08256](https://arxiv.org/abs/2607.08256)
9. **Under a unified self-play + MCTS pipeline on Connect Four (solved, exact game-theoretic...** · score 0.020 · [2607.08984](https://arxiv.org/abs/2607.08984)
10. **IrisFlow is a query-based, open-vocabulary flow-matching framework: candidate materials...** · score 0.017 · [2607.08392](https://arxiv.org/abs/2607.08392)
