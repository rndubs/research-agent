# hexgen digest — 2026-08-22

**13** new backlog item(s) in the last 7 day(s).

## New this window

- **HiFi-BRep is a two-stage VAE + latent-diffusion pipeline for B-Rep generation. The topo...** · score 0.093
  - _Contributions:_ The paper proposes HiFi-BRep, a framework addressing both representation and generation brittleness in B-Rep synthesis. It introduces a compact B-Rep representation (Bezier curves/surfaces plus an explicit edge-face incidence matrix) and a topology-aware encoder that eliminates padding noise via learnable queries and prevents feature contamination via topology-guided (Topo-Mask) attention, yielding a high-fidelity fixed-length latent. It also designs a single-stage decoder that jointly predicts geometry and topology in parallel and enforces the manifold 'two faces per edge' constraint via a differentiable row-wise two-peak objective, rather than deferring validity to post-processing. Experiments show state-of-the-art structural validity and geometric fidelity versus BRepGen, BrepDiff, and DTGBrepGen, plus faster inference.
  - _Applicable to our work:_ Highly applicable to hexgen's B-rep encoder and structural-validity goals: (1) the Topo-Mask idea -- restricting cross-entity attention to only topologically adjacent face/edge pairs via a hard attention mask -- is directly transferable to hexgen's graph-transformer encoder over the B-rep face-adjacency graph, potentially sharpening pointer/copy-head addressing by preventing irrelevant cross-face attention; (2) the row-wise two-peak differentiable-validity-objective pattern (turning a hard combinatorial validity rule -- 'each edge has exactly 2 faces' -- into a supervised, differentiable training signal rather than a post-hoc filter) is a template for embedding some of hexgen's own frozen-verifier checks (e.g. simpler structural ones like feature-edge capture or element-budget sanity) directly into training rather than relying solely on best-of-N + reject; (3) their single-stage parallel joint geometry+topology decoding vs. cascaded generation ablation (Valid 95.2%->69.3% when reverting to cascaded) is direct evidence that jointly generating structure and parameters (as hexgen's pointer/coordinate-vocabulary decoder already does) outperforms sequential decomposition, reinforcing hexgen's existing single-decoder design choice.
  - _Additional context:_ This is the closest paper of the four to hexgen's actual domain (CAD B-Rep generation) and the highest-confidence match. Code and pretrained models are public (github.com/1nnoh/HiFi-BRep), so architectural details (Topo-Mask implementation, row-wise two-peak loss) could be inspected directly rather than re-derived from the paper text. Note their B-reps are unconditional/class-conditional generative targets (~6-29 faces, similar scale to hexgen's 7-15 faces) rather than a program/DSL output, so this bears on the ENCODER and validity-loss side of hexgen, not the decoder's program-generation grammar.
  - _Impact_ 0.75 · _Applicability_ 1.00 · _Confidence_ 0.68 · _Effort_ 5.5/10
  [2608.16485](https://arxiv.org/abs/2608.16485)
- **TDD-Agent is a single agent operationalizing test-driven development: Phase 1 (test-fir...** · score 0.067
  - _Contributions:_ The paper proposes TDD-Agent, which adapts test-driven development to LLM code generation by treating test generation as a reasoning step performed before implementation rather than a fixed post-hoc validator. It shows on LiveCodeBench (via a lightweight TDD-prompt variant) that test-first reasoning alone consistently improves over CoT/SCoT/Self-Planning/ICoT baselines. On the repository-level RepoEval benchmark, the full TDD-Agent framework, which additionally allows tests themselves to be revised jointly with code across iterations, consistently outperforms retrieval-based (RAG, RepoCoder) and agent-based (mini-SWE-agent) baselines.
  - _Applicable to our work:_ The dual-track refinement loop -- iteratively revising BOTH the generated artifact and its own self-check (here: tests; for hexgen: candidate program) using execution/verifier feedback, rather than treating the verifier as a fixed post-hoc filter -- is a directly transferable pattern for repair/edit distillation (care area 1): after a best-of-N candidate program fails the frozen verifier, feed the specific verifier failure signal (e.g. which check failed: compiles / inverted elements / Jacobian bound / surface fidelity) back to the model for targeted repair, and track iteration-level improvement the way this paper tracks pass-rate-per-iteration, rather than only doing flat resampling.
  - _Additional context:_ Domain is software/code generation, not geometry, and the 'test' side of the dual-track loop has no clean hexgen analogue (our verifier is frozen and machine-checkable, not something the model generates and can revise) -- so only the code-repair-via-execution-feedback half transfers directly, not the co-evolving-oracle half. Also worth noting: the paper's own failure analysis (matched failures = self-consistent but wrong) is a caution against letting a model-generated proxy substitute for the real frozen verifier, which reinforces hexgen's existing design of keeping the verifier frozen and external.
  - _Impact_ 0.50 · _Applicability_ 0.60 · _Confidence_ 0.67 · _Effort_ 3.0/10
  [2608.16742](https://arxiv.org/abs/2608.16742)
- **GenMatch maps a full dispatch batch directly to an assignment sequence via an encoder-d...** · score 0.062
  - _Contributions:_ The paper proposes GenMatch, stated to be the first end-to-end generative framework for ride-hailing Micro-View Order-Dispatching deployed in a production environment, replacing a multi-stage predict-then-match pipeline with a single encoder-decoder model that autoregressively generates the full batch assignment. It reports consistent improvements over competitive baselines in offline evaluation and online A/B tests across five cities in DiDi's international markets.
  - _Applicable to our work:_ The State-Aware Pointer Decoder -- which autoregressively points into a graph's node set while dynamically masking candidates invalidated by prior selections -- is a close structural analogue to hexgen's pointer/copy heads addressing faces of the input B-rep graph: GenMatch's evolving-state masking mechanism (removing already-claimed nodes/edges from the pointer's feasible set at each decoding step) is directly transferable to enforcing hexgen's own structural constraints during autoregressive decoding, e.g. masking already-consumed faces or geometrically infeasible next-commands, which bears on care area 1 (structural validity) and area 4 (pointer/graph-transformer mechanisms).
  - _Additional context:_ Deployed at production scale (DiDi, five cities), so the pointer-decoder-plus-dynamic-masking design has been stress-tested for latency and reliability under strict batch-level latency constraints, which is reassuring evidence for the masking idea -- though the application domain (bipartite driver-order matching) is unrelated to CAD/mesh generation, and the datasets/metrics are proprietary production data, not released.
  - _Impact_ 0.45 · _Applicability_ 0.66 · _Confidence_ 0.42 · _Effort_ 2.0/10
  [2608.19751](https://arxiv.org/abs/2608.19751)
- **A two-phase neurosymbolic agent: Phase I couples a VLM to a deterministic exploration h...** · score 0.059
  - _Contributions:_ The paper presents a two-phase architecture separating task-directed visual state acquisition from goal-directed symbolic planning through a narrow grounded-state interface. It introduces a state-dependent decoding procedure that enforces PDDL action applicability at the token level, coupled with language-guided MCTS over executable plan prefixes, guaranteeing plans are executable by construction under the transition model. It empirically validates the approach on VirtualHome and ALFWorld against direct visual and embodied-model baselines, with component ablations, cost analysis, and causal failure attribution.
  - _Applicable to our work:_ The state-dependent constrained decoding + MCTS-over-executable-prefixes pattern is close to directly transferable to hexgen: mask the decoder's token distribution at each step to only tokens/pointer targets that keep the partial hex-meshing program compileable/executable under the deterministic mesher's grammar (analogous to PDDL applicability), then use token/step-level MCTS with PUCB (LM prior + a value from partial verifier checks, e.g. partial Jacobian/element-budget feasibility) to search over executable program prefixes rather than relying on flat best-of-N sampling after the fact. Their component ablation (Table 2, constraints-alone vs. search-alone vs. combined) is a template for validating whether constrained decoding or MCTS (or both) is where hexgen's structural-validity gains actually come from.
  - _Additional context:_ Domain is household robot planning (PDDL/VirtualHome/ALFWorld), not CAD, so transfer is purely at the decoding/search-architecture level. Note the validity guarantee is conditional on domain-model soundness and correct instance grounding -- for hexgen the analogous caveat is that constrained decoding only guarantees syntactic/grammar validity, not full verifier pass (quality bounds, surface-match fidelity still need best-of-N/MCTS value guidance as this paper does with its planning heuristic). Ablation Table 2 is worth replicating in miniature for hexgen before committing engineering effort.
  - _Impact_ 0.70 · _Applicability_ 0.90 · _Confidence_ 0.47 · _Effort_ 5.0/10
  [2608.16794](https://arxiv.org/abs/2608.16794)
- **RGA-Designer fine-tunes a pretrained autoregressive graph generator (ARG-Designer) usin...** · score 0.057
  - _Contributions:_ The paper introduces RGA-Designer, which fine-tunes a pretrained autoregressive multi-agent-topology graph generator (ARG-Designer) against a learned reward model that jointly scores task correctness and structural compactness, via an RLHF-style GRPO policy-optimization stage. Across six benchmarks it reduces token consumption by an average of 20.5% relative to ARG-Designer while preserving task accuracy.
  - _Applicable to our work:_ RGA-Designer's recipe -- take a pretrained autoregressive graph generator, train a separate reward model over generated graphs (here: task-correctness + structural-compactness), then RL-fine-tune the generator against that reward with GRPO -- maps directly onto hexgen: our frozen verifier's pass/fail plus quality-bound margins (scaled-Jacobian, aspect ratio, element budget, boundary fidelity) could be distilled into a learned reward model, and hexgen's pointer-based graph-conditioned decoder could then be GRPO-fine-tuned against it to push beyond what supervised imitation alone reaches -- directly serving care area 6 (verifier-in-the-loop training) and area 1 (structural validity via RL fine-tuning).
  - _Additional context:_ The predecessor ARG-Designer's node-then-edges autoregressive generation conditioned on a partial graph is structurally close to hexgen's own graph-conditioned autoregressive decoding, which strengthens transferability despite the unrelated application domain (multi-agent LLM topology design vs. CAD/mesh programs). Code is stated to be released.
  - _Impact_ 0.60 · _Applicability_ 0.78 · _Confidence_ 0.61 · _Effort_ 5.0/10
  [2608.20099](https://arxiv.org/abs/2608.20099)
- **TUP is a rank-based target policy for Best-of-N-style distillation that removes low-ran...** · score 0.020
  - _Contributions:_ The paper proposes TUP (Truncate-bad, Upweight-good Policy), a Best-of-N distillation method that decouples lower-tail truncation from upper-tail reweighting sharpness, admits a closed-form prompt-independent normalization, and trains via a binary cross-entropy objective purely from offline data. It gives theoretical support showing hard lower-tail truncation can match the best monotone rank-reweighting policy under an oracle reward, and shows TUP is competitive with strong offline alignment baselines on the QRPO benchmark.
  - _Applicable to our work:_ TUP's core mechanism -- distilling Best-of-N selection into a single policy by hard-truncating low-ranked candidates from the training target while softly upweighting the retained top tail -- is directly applicable to hexgen's own best-of-N + frozen-verifier screening: instead of only using the verifier to pick a winner per B-rep at inference time, hexgen could train against TUP-style truncated/upweighted targets over its own best-of-N program samples (ranked by verifier pass/fail plus quality-bound margins) to amortize the best-of-N search into the base policy itself, directly serving care area 6 (self-play / verifier-in-the-loop training, best-of-N self-improvement flywheels).
  - _Additional context:_ Evaluated only on language-model chat/instruction-following alignment (Llama/Mistral on UltraFeedback/Magpie Air) with continuous learned reward models, not on structured/program generation or a hard pass/fail verifier like hexgen's -- the truncation-threshold semantics (a win-rate cutoff) may need rethinking when the underlying signal is a binary verifier plus continuous quality margins rather than a single scalar reward model score.
  - _Impact_ 0.45 · _Applicability_ 0.48 · _Confidence_ 0.57 · _Effort_ 6.0/10
  [2608.19748](https://arxiv.org/abs/2608.19748)
- **AutoSR builds a branching tree of 'Research States' -- each a persistent record of a ca...** · score 0.018
  - _Contributions:_ The paper introduces Research-Space Symbolic Regression and formulates Research-State Search as the underlying sequential decision problem, the first such formulation. It implements AutoSR, combining persistent Research States, independent proposer/reviewer roles, and PW-MCTS-based search, and demonstrates exact algebraic recovery on nine selected challenge equations across two benchmark suites, including three cp3-bench problems no prior published system recovered.
  - _Applicable to our work:_ The core search architecture -- MCTS with progressive widening over persistent search states, percentile-normalized rewards, and a separate reviewer role that GATES acceptance on hard requirements rather than trading them for fit -- is directly transferable to hexgen's best-of-N + frozen-verifier setup: replace flat best-of-N sampling of full programs with an MCTS over partial hexmesh-DSL programs where the frozen verifier plays the reviewer's role (hard pass/fail on compiles/zero-inverted-elements/etc.) and only requirement-satisfying children are retained/expanded; branch-local memory of failed attempts (with their verifier diagnostics) is also a natural fit for DAgger-style repair distillation.
  - _Additional context:_ This is a symbolic-regression paper, not CAD/geometry, so the transfer is purely architectural (MCTS + persistent-state + verifier-gated acceptance), not domain-specific. The evaluation is admittedly small-N/cherry-picked and lacks ablations, so the MCTS-over-Research-States idea should be treated as promising but unvalidated at scale -- would want our own ablation of progressive-widening hyperparameters (kpw, beta) before adopting.
  - _Impact_ 0.45 · _Applicability_ 0.48 · _Confidence_ 0.67 · _Effort_ 8.0/10
  [2608.16876](https://arxiv.org/abs/2608.16876)
- **Marionette factors the world model into three parts: a two-stage autoregressive dynamic...** · score 0.014
  - _Contributions:_ The paper proposes explicitly modeling the evolving world state and delegating exact geometric computation to a fixed, zero-parameter renderer while letting only the neural model handle appearance, instantiated as Marionette for articulated multi-character games. It shows the predicted state is directly controllable (forcing a mismatched action stream changes joint error by 31%) and that long-horizon failures (character drift, ground penetration) are properties of the state that can be repaired by rules imposed on the state alone, without touching the observation model. Routing appearance through the predicted state costs no detectable fidelity versus an end-to-end pixel-autoregressive baseline.
  - _Applicable to our work:_ The core offloading principle -- predict a small, structured, interpretable state with a neural model and delegate all exact geometric computation to a fixed, zero-parameter deterministic operator -- is structurally identical to hexgen's split between a neural program-emitting decoder and a deterministic mesher executor. Their finding that state infeasibility (drift, penetration) is best repaired by projecting/clamping the *interpretable intermediate state* back into a feasible region via explicit rules, rather than through differentiable training-time penalties (which the optimizer actively traded away), is directly relevant to hexgen's verifier-guided generation: it argues for post-hoc feasibility projection/repair on decoded program parameters as a more reliable lever than soft training losses alone. The two-stage decomposition (compact discrete decision model + larger continuous execution model) also mirrors a possible split between command-token selection and coordinate/parameter regression heads in hexgen's decoder.
  - _Additional context:_ This is a game-world-model paper, not CAD/geometry program synthesis -- the applicability is a mechanism-level analogy (explicit interpretable state + deterministic renderer) rather than a directly reusable component. Code is promised but not yet confirmed live at review time; the WildWorld dataset is already public. Worth revisiting once code lands, since the state-projection-as-repair idea could be prototyped directly against hexgen's frozen verifier.
  - _Impact_ 0.35 · _Applicability_ 0.36 · _Confidence_ 0.67 · _Effort_ 6.0/10
  [2608.14530](https://arxiv.org/abs/2608.14530)
- **Formulates nested SMC (NSMC) and fully-adapted nested SMC (FA-NSMC) for Feynman-Kac rew...** · score 0.013
  - _Contributions:_ The paper develops properly-weighted nested SMC (NSMC) and fully-adapted NSMC (FA-NSMC) algorithms for Feynman-Kac reward-steering of discrete diffusion language models, identifying and fixing a systematic bias in a prior nested-SMC diffusion-guidance formulation. It empirically shows NSMC and FA-NSMC consistently outperform best-of-N and bootstrap SMC baselines on toxicity- and fluency-steering tasks for a masked diffusion language model.
  - _Applicable to our work:_ NSMC/FA-NSMC's Feynman-Kac steering -- spawning an inner SMC per outer particle to approximate the locally optimal proposal at each generation step -- is a transferable search strategy for our best-of-N + frozen-verifier screening: it could augment or replace plain best-of-N sampling with a properly-weighted particle-filtering search over program prefixes, using the verifier's pass/fail and quality-bound margins as the steered reward, potentially finding valid/high-quality hex-meshing programs more sample-efficiently than best-of-N alone (directly serving care area 1: execution/verifier-guided generation and search over program prefixes).
  - _Additional context:_ Targets masked discrete-diffusion LMs, not autoregressive decoders like hexgen's -- but SMC-style steering over sequence prefixes is decoder-family-agnostic in principle (classical SMC steering of autoregressive LMs is well established), so the nested-proposal correctness fix here is the transferable contribution rather than the diffusion-specific machinery. Directly benchmarks against best-of-N, which is exactly hexgen's current screening strategy, making this a natural comparison point. Only the abstract/introduction were available for this extraction; results/limitations sections were not inspected in depth.
  - _Impact_ 0.50 · _Applicability_ 0.42 · _Confidence_ 0.38 · _Effort_ 6.0/10
  [2608.20123](https://arxiv.org/abs/2608.20123)
- **Block3D partitions a discrete shape-token sequence into contiguous blocks, generates bl...** · score 0.012
  - _Contributions:_ The paper introduces Block3D, a block-wise autoregressive-diffusion hybrid for text-to-3D shape generation that shifts causal dependency from individual shape tokens to contiguous latent blocks, paired with a confidence-guided intra-block correction mechanism that revises low-confidence tokens before a block is finalized. On a held-out TRELLIS-500K set it reduces mean end-to-end generation time from 25.71s to 4.99s (a 5.15x speedup) relative to a fine-tuned autoregressive baseline while achieving the best geometric fidelity among evaluated methods.
  - _Applicable to our work:_ Block3D's confidence-guided intra-block correction -- letting the decoder revise its own low-confidence emitted tokens before committing a block, rather than being purely causal and irrevocable like standard autoregressive decoding -- is directly relevant to care area 1 (structural validity of generated programs): hexgen's autoregressive DSL-program decoder currently cannot revise an earlier bad command choice, and a block-wise generate-then-correct scheme applied to short spans of the DSL program could reduce compounding errors that lead to non-compiling or invalid programs, complementing (not replacing) hexgen's existing best-of-N + verifier screening.
  - _Additional context:_ Applied to continuous/discretized 3D shape tokens from a frozen VQ-style tokenizer, not to a structured DSL grammar with hard syntactic/semantic validity constraints like hexgen's -- adapting block-wise joint denoising plus token correction to a strict program grammar (where partial-block edits could transiently produce syntactically invalid states) would need care. A project page and an appendix section describing a released code/data package are mentioned, though the exact code repository URL was not visible in the excerpted text.
  - _Impact_ 0.40 · _Applicability_ 0.42 · _Confidence_ 0.58 · _Effort_ 8.0/10
  [2608.19567](https://arxiv.org/abs/2608.19567)
- **SPADE has a single LLM play two roles with shared weights: an Environment Designer that...** · score 0.012
  - _Contributions:_ The paper introduces SPADE, a self-play RL framework where a single LLM both designs executable training environments as code and learns to solve them, unifying single-turn reasoning and multi-turn agentic tool-use tasks under one Gym-style interface. It proposes a hint-based regret reward that trains the Environment Designer (rather than keeping it frozen) to target environments at the Reasoning Agent's learning frontier, combined with corpus grounding and environment memory, and validates the approach at 30B-parameter scale on both games/reasoning and tool-use domains.
  - _Applicable to our work:_ The hint-based regret mechanism (rewarding a generator for producing tasks that are solvable but hard — measured via the performance gap with vs. without a privileged hint) is a directly transferable idea for our 'inverse factory': instead of sampling uniformly-random valid programs, an adaptive generator could be trained/tuned to bias toward B-rep/program pairs at the current model's difficulty frontier, targeting the domain-gap and data-diversity-vs-volume tradeoff (care area 2). The broader self-play/co-evolution pattern — using execution-verifiable generated tasks with built-in verification code, evolving alongside the learner rather than being frozen — parallels our best-of-N + frozen-verifier setup and suggests exploring a non-frozen, adaptive difficulty curriculum over the verifier-screened training distribution (care area 6, self-play/verifier-in-the-loop training and LLM-agent authoring pipelines as a training-data source).
  - _Additional context:_ Domain and modality (language-agent RL on math/code/tool-use tasks, executable-Python environments) differ substantially from hexgen's geometric program synthesis, and SPADE's verifier is not frozen (it co-evolves with the Designer) whereas our verifier is explicitly frozen and machine-checkable — so the regret-based curriculum idea would need adaptation, not our reward function itself. Long, appendix-heavy paper (36+ pages including extended ablations, theoretical regret analysis, and generated-environment galleries); this extraction is grounded in the abstract, introduction, related work, and discussion/limitations sections only, not the full experimental/appendix detail. Code released at https://github.com/spade-rl/spade.
  - _Impact_ 0.35 · _Applicability_ 0.36 · _Confidence_ 0.65 · _Effort_ 7.0/10
  [2608.19197](https://arxiv.org/abs/2608.19197)
- **AlphaClifford is a model-based RL framework that represents Clifford circuits as symple...** · score 0.011
  - _Contributions:_ The paper introduces AlphaClifford, a model-based RL framework using Monte Carlo Tree Search over the symplectic-matrix representation of Clifford circuits to synthesize short gate sequences from the {H,S,CNOT} gate set. It demonstrates the framework on three tasks: unconstrained circuit optimization, hardware-connectivity-constrained transpilation, and as a post-synthesis optimizer within a full Clifford+T logical synthesis pipeline, outperforming heuristic and prior RL-based baselines on gate-count reduction in most settings.
  - _Applicable to our work:_ The core mechanism — modeling program synthesis as tree search (MCTS with paired policy/value networks, UCT selection) over a structured state space defined by algebraic/execution equivalence, with a target-state 'inverted' goal-reaching formulation — is directly transferable to hexgen's problem of emitting a valid DSL program: a policy+value-guided MCTS over partial hex-meshing programs (rather than pure best-of-N sampling) could improve structural validity and gate/command-count efficiency, mirroring care area 1 (value/critic-guided search, MCTS over program prefixes). The curriculum-learning strategy (increasing target-circuit complexity, advancing once a 95% solve-rate threshold is met) is also a directly borrowable recipe for scheduling the difficulty of inverse-factory-generated training programs.
  - _Additional context:_ Domain is quantum circuit compilation, not CAD/geometry, and the action space (three discrete Clifford gates) and state space (linear algebra over GF(2)) are far simpler and more structured than B-rep/mesh program generation, so the exact machinery won't port directly — but the search-over-program-space design pattern is a clean, well-validated analogue worth studying alongside prior AlphaTensor/AlphaCNOT work this paper builds on. No public code link found in the extracted text.
  - _Impact_ 0.45 · _Applicability_ 0.42 · _Confidence_ 0.47 · _Effort_ 8.0/10
  [2608.18946](https://arxiv.org/abs/2608.18946)
- **HI-MeshGraphNets (HI-MGN) replaces the flat GNN processor of MeshGraphNets with a hiera...** · score 0.007
  - _Contributions:_ The paper proposes a hierarchical multiscale extension of MeshGraphNets that coarsens simulation meshes via FPS and Voronoi partitioning to enable long-range message passing with fewer layers, then reconstructs fine-resolution features with a learned interpolation network. Across three structural and fluid benchmarks (2D thermoelastic, 3D nonlinear contact, 3D aerodynamic), the method improves accuracy relative to MeshGraphNets and Bi-Stride Multi-Scale GNN (BSMS-GNN) at a comparable number of message-passing blocks, while reducing training time and peak memory usage. The authors argue this demonstrates topology-aware hierarchical processing and learned coarse-to-fine interpolation as a practical route to scalable mesh-based physics surrogates.
  - _Applicable to our work:_ HI-MGN's FPS + Voronoi hierarchical coarsening and learned coarse-to-fine interpolation is a directly transferable mechanism for hexgen's graph-transformer encoder over B-rep face-adjacency graphs: if hexgen's B-rep graphs grow beyond the current ~7-15 faces (or if the mesher-side program needs to reason over finer element-level topology), the same coarsen/message-pass/interpolate recipe could extend the encoder's effective receptive field over graph hops without deepening the transformer, and it explicitly preserves topology rather than introducing artificial adjacency -- a property that matters for a domain where boundary fidelity and feature-edge capture are verifier-checked.
  - _Additional context:_ This is a physics-simulation surrogate paper (structural/fluid FEM-style meshes), not CAD B-rep or program synthesis -- relevance is via the shared substrate (graph/mesh-structured data) and multiscale message-passing mechanism, not the task itself. Extraction is based on the abstract/introduction; the numbered results tables in Sections 3-4 were not visible in the supplied text, so quantitative deltas beyond the qualitative headline claim are not confirmed here.
  - _Impact_ 0.25 · _Applicability_ 0.42 · _Confidence_ 0.38 · _Effort_ 6.0/10
  [2608.13827](https://arxiv.org/abs/2608.13827)

## Current top 10 backlog

1. **HiFi-BRep is a two-stage VAE + latent-diffusion pipeline for B-Rep generation. The topo...** · score 0.093 · [2608.16485](https://arxiv.org/abs/2608.16485)
2. **TDD-Agent is a single agent operationalizing test-driven development: Phase 1 (test-fir...** · score 0.067 · [2608.16742](https://arxiv.org/abs/2608.16742)
3. **GenMatch maps a full dispatch batch directly to an assignment sequence via an encoder-d...** · score 0.062 · [2608.19751](https://arxiv.org/abs/2608.19751)
4. **A two-phase neurosymbolic agent: Phase I couples a VLM to a deterministic exploration h...** · score 0.059 · [2608.16794](https://arxiv.org/abs/2608.16794)
5. **Models the per-instance false-accept rate as a latent variable drawn from a distributio...** · score 0.058 · [2607.13918](https://arxiv.org/abs/2607.13918)
6. **RGA-Designer fine-tunes a pretrained autoregressive graph generator (ARG-Designer) usin...** · score 0.057 · [2608.20099](https://arxiv.org/abs/2608.20099)
7. **For a fixed candidate pool, the paper decomposes a selector or verifier's net gain into...** · score 0.054 · [2607.17531](https://arxiv.org/abs/2607.17531)
8. **3D CAD consensus selection: sample N parametric CAD programs from an LLM, compile each...** · score 0.039 · [2608.09706](https://arxiv.org/abs/2608.09706)
9. **Introduces 'generative compilation': a 'sealor' transformation converts a partial progr...** · score 0.033 · [2607.13921](https://arxiv.org/abs/2607.13921)
10. **The paper proves that a broad class of hierarchical graph transformers combining global...** · score 0.029 · [2607.17570](https://arxiv.org/abs/2607.17570)
